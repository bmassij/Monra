import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Checklist delen — Monra website-audit',
  robots: { index: false, follow: false },
}

export { default } from '../AuditChecklistPage'
