import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Website-audit checklist — Monra (intern)',
  robots: { index: false, follow: false },
}

export { default } from './AuditChecklistPage'
